{
  "name": "arbitrai",
  "private": true,
  "scripts": {
    "simulate": "cd cre-workflow && npm run simulate",
    "simulate:broadcast": "cd cre-workflow && npm run simulate -- --broadcast",
    "simulate:demo": "node scripts/simulate-demo.mjs",
    "seed:evidence": "node scripts/seed-evidence.mjs"
  }
}
